# AbstractAssistant Configuration

[ui]
theme = "dark"  # dark, light, system
bubble_size_ratio = 0.167  # 1/6th of screen
auto_hide_delay = 8  # seconds for toast notifications
always_on_top = true

[llm]
default_provider = "openai"
default_model = "gpt-5-mini"
max_tokens = 128000
temperature = 0.7

[system_tray]
icon_size = 64  # pixels
show_notifications = true
animation_fps = 30  # smoother icon animation (10-30)

[shortcuts]
show_bubble = "cmd+shift+a"  # Global hotkey to show chat bubble


[gateway]
use_gateway = true
url = "http://127.0.0.1:8081"
auth_token = "my-security-is-paramount-so-1980"
bundle_id = "basic-agent"


# API Keys should be set via environment variables:
# OPENAI_API_KEY=your_key_here
# ANTHROPIC_API_KEY=your_key_here
